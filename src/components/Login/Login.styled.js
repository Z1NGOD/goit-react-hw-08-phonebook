import styled from '@emotion/styled';
const FormContainer = styled.div`
  width: 300px;
  margin: 0 auto;
`;

const Form = styled.form`
  display: flex;
  flex-direction: column;
  gap: 10px;
`;

const Label = styled.label`
  font-weight: bold;
`;

export { FormContainer, Form, Label};
